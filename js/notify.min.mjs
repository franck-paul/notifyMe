export {notifyBrowser,};function notifyBrowser(msg, title = 'Dotclear', silent = !1, wait = !1){var notify_options={body:msg,icon:'images/favicon.ico',silent,requireInteraction:wait,};if('Notification'inwindow) {if(Notification.permission==='granted') {var notification=new Notification(title, notify_options);if(wait===!1) {setTimeout(notification.close.bind(notification), 4000);}}elseif(Notification.permission!=='denied') {Notification.requestPermission((permission)=>{if(!('permission'inNotification)) {Notification.permission = permission;}if(permission==='granted') {var _notification=new Notification(title, notify_options);if(wait===!1) {setTimeout(_notification.close.bind(_notification), 4000);}}});}}}
